name: CMake
on:
  push:
    paths:
      - '**/*.cmake'
      - '**/*.cmake.in'
      - '**/CMakeLists.txt'
      - '.github/workflows/cmake.yml'
      - 'Brewfile'
      - 'ci/**'
      - 'cmake/**'
      - 'compose.yaml'
      - 'dockerfiles/*'
      - 'include/**/*.h'
      - 'include/**/*.h.in'
      - 'include/**/*.hpp'
      - 'lib/**/*.c'
      - 'lib/**/*.cpp'
      - 'lib/**/*.h'
      - 'lib/**/*.hpp'
      - 'lib/**/*.rb'
      - 'plugins/**/*.c'
      - 'plugins/**/*.cpp'
      - 'plugins/**/*.h'
      - 'plugins/**/*.rb'
      - 'setup.sh'
      - 'src/**/*.c'
      - 'src/**/*.h'
      - 'test/command/**'
      - 'test/command_line/**'
      - 'test/mruby/**'
      - 'vendor/mruby/**'
  pull_request:
    paths:
      - '**/*.cmake'
      - '**/*.cmake.in'
      - '**/CMakeLists.txt'
      - '.github/workflows/cmake.yml'
      - 'Brewfile'
      - 'ci/**'
      - 'cmake/**'
      - 'compose.yaml'
      - 'dockerfiles/*'
      - 'include/**/*.h'
      - 'include/**/*.h.in'
      - 'include/**/*.hpp'
      - 'lib/**/*.c'
      - 'lib/**/*.cpp'
      - 'lib/**/*.h'
      - 'lib/**/*.hpp'
      - 'lib/**/*.rb'
      - 'plugins/**/*.c'
      - 'plugins/**/*.cpp'
      - 'plugins/**/*.h'
      - 'plugins/**/*.rb'
      - 'setup.sh'
      - 'src/**/*.c'
      - 'src/**/*.h'
      - 'test/command/**'
      - 'test/command_line/**'
      - 'test/mruby/**'
      - 'vendor/mruby/**'
  schedule:
    - cron: |
        0 21 * * *
concurrency:
  group: ${{ github.head_ref || github.sha }}-${{ github.workflow }}
  cancel-in-progress: true
jobs:
  windows-msys2:
    name: Windows MSYS2
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - msystem: MINGW64
            package-prefix: mingw-w64-x86_64
            runner: windows-latest
          - msystem: UCRT64
            package-prefix: mingw-w64-ucrt-x86_64
            runner: windows-latest
          - msystem: CLANGARM64
            package-prefix: mingw-w64-clang-aarch64
            runner: windows-11-arm
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: actions/checkout@v6
        with:
          repository: msys2/MINGW-packages
          path: ci/msys2/MINGW-packages
      - uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          update: true
          install: >-
            ${{ matrix.package-prefix }}-ccache
            ${{ matrix.package-prefix }}-toolchain
            ${{ matrix.package-prefix }}-ruby
            ${{ matrix.package-prefix }}-xsimd
            base-devel
            binutils
            git
            msys2-devel
            pactoys
            ${{ matrix.package-prefix }}-gobject-introspection
            ${{ matrix.package-prefix }}-glib2
            ${{ matrix.package-prefix }}-pkgconf
      - name: Prepare
        shell: msys2 {0}
        run: |
          set -ex
          cd ci/msys2
          sed -i'' -e 's/^CheckSpace/#CheckSpace/g' /etc/pacman.conf
          MINGW-packages/.ci/fetch-validpgpkeys.sh
      - name: Prepare ccache
        shell: msys2 {0}
        run: |
          echo "CCACHE_DIR=${PWD}/ccache" >> ${GITHUB_ENV}
      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ccache
          key: cmake-windows-msys2-${{ matrix.msystem }}-ccache-${{ hashFiles('lib/**', 'src/**', 'plugins/**', 'include/**') }}
          restore-keys: cmake-windows-msys2-${{ matrix.msystem }}-ccache-
      - name: Build
        shell: msys2 {0}
        run: |
          set -ex
          version=$(cat base_version)
          rake dist
          mv groonga-${version}.tar.gz ci/msys2/
          cd ci/msys2
          sed -i'' -e "s/^pkgver=/pkgver=${version}/" PKGBUILD
          MINGW_ARCH=${{ matrix.msystem }} \
            makepkg-mingw \
              --cleanbuild \
              --nocheck \
              --noconfirm \
              --noprogressbar \
              --rmdeps \
              --syncdeps
          ccache --show-stats --verbose --version || :
      - name: Install
        shell: msys2 {0}
        run: |
          set -ex
          cd ci/msys2
          pacman \
            --noconfirm \
            --noprogressbar \
            --upgrade \
            *.pkg.tar.*
          cp -a *.pkg.tar.* ../../

      # Artifact
      - uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.msystem }}
          path: "*.pkg.tar.*"

      # Test
      - name: Use JST
        run: |
          Set-TimeZone -Id "Tokyo Standard Time"
      - name: Install test dependencies
        shell: msys2 {0}
        run: |
          gem install rubygems-requirements-system
          # "fiddle" is for ensuring using the installed system libffi
          MAKEFLAGS="-j$(nproc) V=1" gem install \
            fiddle \
            grntest \
            pkg-config \
            red-arrow || :
          echo "=== Debug: pkg-config command (system) ==="
          echo "--- arrow ---"
          pkg-config --cflags arrow
          echo "--- arrow-glib ---"
          pkg-config --cflags arrow-glib
          echo "--- arrow arrow-glib ---"
          pkg-config --cflags arrow arrow-glib
          echo "=== Debug: PKGConfig gem (Ruby) ==="
          ruby -e "require 'pkg-config'; puts PKGConfig.cflags('arrow')"
          ruby -e "require 'pkg-config'; puts PKGConfig.cflags('arrow-glib')"
          echo "=== Debug: end ==="
          ruby -e "require 'arrow'"
      - name: Prepare setup-extpp.rb
        shell: msys2 {0}
        run: |
          cat <<RUBY > setup-extpp.rb
          ["extpp"].each do |gem|
            spec = Gem::Specification.find_by_name(gem)
            ENV["PATH"] = [
              File.join(spec.full_gem_path, "lib"),
              ENV["PATH"],
            ].join(File::PATH_SEPARATOR)
          end
          RUBY
      - name: "Test: command line"
        shell: msys2 {0}
        run: |
          ruby test/command_line/run-test.rb
      - name: "Test: stdio"
        shell: msys2 {0}
        run: |
          ruby \
            -r./setup-extpp \
            -S \
              grntest \
                --base-directory=test/command \
                --n-retries=2 \
                --read-timeout=30 \
                --reporter=mark \
                --timeout=60 \
                test/command/suite
